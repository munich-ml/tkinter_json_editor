[project]
name = "tree-edit-frame"
version = "0.1.0"
description = "A reusable Tkinter component for editing hierarchical data structures"
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"
